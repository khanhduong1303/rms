<% if @reply.errors.any? %>
  
<% else %>
var className = '.feedback-<%= @reply.feedback.id %> p' ;
  $('.feedback-<%= @reply.feedback.id %> div.reply p').text('<%= @reply.content %>');
  $('#feedback-modal').modal('hide');

<% end %>

