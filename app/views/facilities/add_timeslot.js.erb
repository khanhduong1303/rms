<% if @timeslot.errors.any? %>
alert("Please Choose the time start and end");
<% else %>
<%  @timeslot.peak = false
     @timeslot.save   %>
$('table.content tbody').html('<%= escape_javascript(render "content_timeslot") %>')
("input").val('');
<% end %>

