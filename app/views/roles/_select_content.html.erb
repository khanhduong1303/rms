<select class="form-control">
    <option value="nil" id="nil"  'selected' %>
      --Select a role--
    </option>
    <% @roles.each do |r| %>
        <option  id="<%= r.id %>" value="<%= r.id %>">
        <%= r.role_name %>

        </option>
    <% end %>
  </select>