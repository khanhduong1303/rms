<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Starter Template for Bootstrap</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <meta name="description" content="">
  <meta name="author" content="">

  <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!--[if lt IE 9]>
    <%= javascript_include_tag "https://oss.maxcdn.com/libs/html5shiv/3.7.0/html5shiv.js", "https://oss.maxcdn.com/libs/respond.js/1.3.0/respond.min.js" %>
  <![endif]-->

  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class="container">

  <div class="row">
    <div class="col-lg-3">
      <%= image_tag 'innoria_logo2.png' %>
    </div>
   <div class="col-lg-9">
     <div class="float-right">
       <a href="">Welcome </a>
       <% #if user_signed_in? %>
        <% #= current_user.email %>
        <% #end %>
       <a href="" class="glyphicon glyphicon-cog"></a>
       <input type="button" class="btn btn-primary" value="Sign out"/>
     </div>

   </div>
  </div>
   <div class="row paddingTop20px">
     <div class="col-lg-12" style="">
       <!--<div class="btn-group with100per " data-toggle="buttons">-->
         <a href="/homes"  class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='home'%><%= 'active' %><%end %>">Home</a>
         <a href="/bookings" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='bookings'%><%= 'active' %><%end %>">Bookings</a>
         <a href="/bulletins" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='bulletins'%><%= 'active' %><%end %>">Bulletin</a>
       <a href="/condo_infos" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='condoInfos'%><%= 'active' %><%end %>">Condo Info</a>
       <a href="/events" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='events'%><%= 'active' %><%end %>">Events</a>
       <a href="/facilities" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='facilities'%><%= 'active' %><%end %>">Facilities</a>
       <a href="/feedbacks" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='feedbacks'%><%= 'active' %><%end %>">Feedback</a>
       <a href="/forms" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='forms'%><%= 'active' %><%end %>">Forms</a>
       <a href="/guard_houses" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='guard_houses'%><%= 'active' %><%end %>">Guard House</a>
       <a href="/house_rules" class="btn btn-primary with10per menuHome floatLeft <%if session[:menustatus]=='house_rules'%><%= 'active' %><%end %>">House Rules</a>
       <!--</div>-->
     </div>
   </div>

    <% flash.each do |name, msg| %>
      <%= content_tag :div, :class => "alert alert-#{ name == :error ? "danger" : "success" } alert-dismissable" do %>
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <%= msg %>
      <% end %>
    <% end %>
  <div class="row paddingTop20px">
    <%= yield %>
  </div>
  <div class="footer">
    <p>© Innoria Company</p>
  </div>
 </div>
</body>
</html>
