<% if @user.errors.any? %>
var error = $('<div id="errors"><p>Please correct the following <%= pluralize(@user.errors.count, "error") %>:</p><ul><% @user.errors.full_messages.each do |msg| %><li><%= msg %></li><% end %></ul></div>')
$("div.errors").append(error);
<% else  %>

alert("Change pass Success");

<% end %>
