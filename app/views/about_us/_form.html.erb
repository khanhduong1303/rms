<%= form_tag(about_us_path, remote:true) do |f| %>
  <% if @about_u.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@about_u.errors.count, "error") %> prohibited this about_u from being saved:</h2>
      <ul>
      <% @about_u.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <fieldset>
      <a><%= submit_tag  'Create About Us', class:'legend' %></a>
      <div class="col-md-12">
        <%= label :about_u, :information %>
        <br>
        <%= text_area_tag 'about_u[information]',nil, class: 'form-control' %>
      </div>
    </fieldset>
<% end %>
